import type { MapInsets } from '@campus-map/mapkit';

/**
 * Сколько места по краям карты занимает интерфейс поверх неё, CSS-пиксели.
 *
 * План, маршрут и «показать план целиком» вписываются с этими отступами:
 * иначе план уходил под шапку, колонку этажей и нижнюю карточку, а подогнанный
 * маршрут начинался под поисковой строкой. Числа соответствуют раскладке в
 * `index.css` (`.campus-map-header`, `.campus-map-rail`, нижняя карточка) и
 * меняются вместе с ней.
 */
export const MAP_CHROME_INSETS: MapInsets = Object.freeze({ top: 72, right: 64, bottom: 112, left: 16 });
