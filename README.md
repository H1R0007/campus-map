# Карта кампуса

Монорепо двух веб-приложений над общим доменным ядром:

- **Навигатор** (`apps/viewer`) — то, что видит студент: планы этажей, поиск
  аудитории, маршрут с пошаговыми инструкциями. PWA, работает офлайн.
- **Редактор** (`apps/editor`) — внутренний инструмент команды разметки:
  узлы, рёбра, переходы между этажами, алиасы, диагностика данных.

Оба приложения работают с одним каталогом данных `data/` в корне репозитория.

Документ рассчитан на разработчика, который получает задачу и должен понять,
где именно менять код и какие ограничения при этом нельзя нарушать. Раздел
«Как поставить задачу» в конце — про это.

Соседние документы:

- `DECISIONS.md` — журнал архитектурных решений «контекст → решение →
  последствия». Читать, прежде чем отменять что-то, что выглядит странно:
  почти за каждым ограничением здесь стоит конкретный дефект.
- `docs/archive/` — исторические документы. **Не список задач**: аудит от
  сентября 2026 описывает состояние до серии рефакторингов и по большей части
  уже неактуален.

---

## Быстрый старт

Требуется Node 22 и pnpm 9 (`packageManager` в `package.json` задан явно).

```bash
pnpm install     # зависимости + линковка workspace-пакетов
pnpm dev         # оба приложения: навигатор на :3000, редактор на :3001
```

`pnpm dev` сначала собирает пакеты (`turbo dev` зависит от `^build`), поэтому
отдельно собирать ядро не нужно.

Остальные команды:

| Команда | Что делает |
| --- | --- |
| `pnpm build` | Собирает пакеты в `dist` и приложения в `apps/*/dist` |
| `pnpm typecheck` | Проверка типов во всех пакетах |
| `pnpm lint` | ESLint (плоский конфиг в корне) |
| `pnpm test` | Тесты ядра, навигатора и редактора (Vitest), сверка `dist/data` с `data/` и смоук-тесты раздачи обоих приложений |
| `pnpm check:browser` | Сценарии навигатора и редактора в браузере без окна — после `pnpm build` (раздел «Проверки») |
| `pnpm clean` | Удаляет `dist` и `*.tsbuildinfo` во всех пакетах |
| `pnpm clean:all` | То же плюс `node_modules` |

Проверять всё сразу: `pnpm build && pnpm typecheck && pnpm lint && pnpm test`.
То же самое делает CI (`.github/workflows/ci.yml`).

Все служебные скрипты — на Node (`tooling/`), а не команды оболочки: основная
машина разработки в проекте под Windows, и обход через Git Bash не должен быть
обязательным.

### Куда разворачивается

По умолчанию сборка рассчитана на корень домена. Для подкаталога:

```bash
CAMPUS_BASE_PATH=/map/ pnpm build     # в PowerShell: $env:CAMPUS_BASE_PATH='/map/'
```

Переменная задаёт `base` для Vite; в коде используется
`import.meta.env.BASE_URL` через `src/config/dataBase.ts`. От неё зависят URL
датасета, `start_url` манифеста PWA, `navigateFallback` service worker и правила
кэширования — поэтому собирать «как получится» и надеяться нельзя. CI проверяет
оба варианта на каждом прогоне.

Ещё одна переменная — `CAMPUS_DATA_DIR`: позволяет прогнать приложения на другом
каталоге данных, не трогая канонический `data/`. Набор целевого объёма (5
корпусов по 11 этажей с подвалом, ~2400 узлов, переводы, одноимённые помещения)
строит генератор:

```bash
node tooling/generate-synthetic-campus.mjs --out .local/campus-5x11 [--metric]
```

Он печатает абсолютный путь для `CAMPUS_DATA_DIR` — относительный Vite
разрешил бы от каталога приложения — и проверяет набор ядром: без
предупреждений и связный. Проверка сборки на таком наборе:
`node tooling/verify-dist-data.mjs --app viewer --data .local/campus-5x11`.
После неё обычный `pnpm build` возвращает в `dist` канонический датасет.

---

## Архитектура

```
data/                        датасет: единственный источник истины о кампусе
│
├── packages/core            доменное ядро (без React, Leaflet, DOM, console)
│   ├── types/               контракт данных: MapNode, Transition, Dataset…
│   ├── dataset/             загрузка и нормализация, раскладка путей
│   ├── graph/               неизменяемый граф с индексами, связность
│   ├── pathfinding/         A*, модель стоимости, альтернативные маршруты
│   ├── aliases/             нечёткий поиск по названиям
│   ├── projection.ts        метрическое пространство: привязка планов
│   ├── geometry.ts          расстояние, ключи рёбер и этажей
│   └── scope.ts             область видимости карты (кампус / этаж)
│
├── packages/mapkit          общая обвязка Leaflet-карты
│   ├── PixelMap             один план как подложка + слои поверх
│   └── WorldMap, PlacedPlan холст кампуса в метрах, планы по привязке
│
└── apps/
    ├── viewer               навигатор (React, zustand, PWA)
    └── editor               редактор разметки (React, zustand, immer)
```

### Правило зависимостей

Зависимости идут только внутрь: `apps → mapkit → core`. Ядро не знает ни о
React, ни о Leaflet, ни о сети, ни о файловой системе.

Всё, что связано с вводом-выводом, спрятано за интерфейсом `DatasetSource`:

```ts
interface DatasetSource {
  readJson(path: string): Promise<unknown | null>;  // null — файла нет
}
```

Ядро принимает уже разобранные данные. Три реализации источника:

| Реализация | Где |
| --- | --- |
| `createHttpDatasetSource()` | ядро; используется навигатором и редактором |
| источник поверх JSZip | `apps/editor/src/utils/importZip.ts` |
| источник поверх ФС | `packages/core/tests/helpers/realDataset.ts` |
| источник в памяти | `packages/core/tests/helpers/memorySource.ts` |

Пайплайн нормализации (`loadDataset`) при этом существует в одном
экземпляре. **Нормализовать данные вручную в приложении нельзя**: это ровно
та ошибка, из-за которой раньше в репозитории было три независимых
загрузчика, разошедшихся между собой.

### Ядро подключается собранным, не исходниками

Приложения импортируют `@campus-map/core` и `@campus-map/mapkit` как обычные
пакеты — через поле `exports`, указывающее на `dist`. Алиасов на исходники в
конфигах нет и быть не должно.

Порядок сборки задаёт turbo: задачи `dev`, `typecheck`, `lint` и `test`
зависят от `^build`. Следствие, о котором нужно помнить: **после правки ядра
его нужно пересобрать**, иначе приложения увидят старую версию.

```bash
pnpm --filter @campus-map/core build      # разово
pnpm --filter @campus-map/core dev        # или watch-режим на время работы
```

Условие `types` в `exports` обязано стоять первым — иначе TypeScript
резолвит `.js` вместо `.d.ts` и типы пакета теряются.

### Данные раздаёт плагин, а не `public/`

Датасет лежит в корне и подхватывается автоматически через
`tooling/vite-plugin-campus-data.mjs`: в dev и preview он раздаёт файлы
middleware'ом, в прод-сборке кладёт их в `dist/data/`.

**Копий датасета внутри приложений быть не должно.** Это не пожелание: каталог
`public/` Vite копирует в `dist/` сам, поэтому копия делает Vite вторым
писателем в `dist/data/**` — и кто победит, зависит от порядка. Проявляется это
как «в редакторе видно, в навигаторе нет». Запрет держится тремя средствами:
строка в `.gitignore`, падение сборки в самом плагине и сверка `dist/data` с
`data/` в `tooling/verify-dist-data.mjs` (входит в `pnpm test`). Подробности —
запись 4 в `DECISIONS.md`.

> Если копия оказалась junction или симлинком на Windows, удалять её нужно
> через `rmdir` либо `Directory.Delete(path, false)`. `rm -rf` в Git Bash идёт
> **по ссылке** и уничтожает настоящие данные.

Плагин отвечает на любой запрос к `/data/*` сам и не пропускает его в
SPA-fallback. Это принципиально: Vite на неизвестный путь отдаёт
`index.html` с кодом 200, и тогда «этажа нет» из мягкого предупреждения
загрузчика превращается в падение на разборе HTML как JSON. Отсутствующий
файл — 404, выход за каталог — 403, неразбираемый путь — 400.

---

## Формат данных

### Раскладка

```
data/
├── campus/
│   ├── meta.json         список корпусов + резервный размер карты
│   ├── graph.json        узлы территории кампуса
│   └── map.png | map.svg план территории
├── buildings/<building_id>/
│   ├── meta.json         имя корпуса и список этажей
│   └── floors/<floor>/
│       ├── graph.json    узлы этажа
│       └── map.png | map.svg план этажа
├── transitions.json      переходы между этажами и корпусами
└── aliases.json          человекочитаемые названия узлов
```

Все пути централизованы в `packages/core/src/dataset/paths.ts`
(`buildingMetaPath`, `floorGraphPath`, `floorMapPath`, …) и там же
URL-хелперы (`datasetUrl`, `campusMapUrl`, `floorMapUrl`). **Склеивать
`/data/...` строковым шаблоном в приложении нельзя** — опечатка молча
превращается в 404.

### Формат плана

План территории и каждого этажа — `map.png` или `map.svg`. Формат задаётся
полем `planFormat` (`"png"` или `"svg"`) в `campus/meta.json` и в записи этажа
в `meta.json` корпуса; без поля — PNG. Имя файла фиксировано, выбирается только
формат: произвольное имя файла в данных уже давало молчаливые 404. У SVG
размер плана — атрибуты `width` и `height` корневого элемента, в тех же
единицах, что и координаты узлов. Подробности — запись 29 в `DECISIONS.md`.

### Главное правило: `building` и `floor` определяет путь файла

В `graph.json` у узла записаны только `id`, `x`, `y`, `neighbors`,
`isPortal` и необязательный `comment`. Принадлежность корпусу и этажу
загрузчик берёт из расположения файла:

- `campus/graph.json` → `building: "CAMPUS"`, `floor: 0`;
- `buildings/building_a/floors/3/graph.json` → `building: "building_a"`,
  `floor: 3`.

Поля `building` и `floor`, записанные в JSON, игнорируются (в типе
`MapNodeData` они помечены как устаревшие и нужны только чтобы заметить
расхождение). Экспорт редактора их не пишет.

### Узел

```json
{
  "id": "a1_room101",
  "x": 130,
  "y": 250,
  "neighbors": ["a1_corridor_w"],
  "isPortal": false,
  "comment": "геометрия приблизительная, уточнить у коменданта"
}
```

- `x`, `y` — **пиксели изображения плана**, отсчёт от левого верхнего угла,
  ось y вниз. Карта работает в такой же системе (`PLAN_CRS` в mapkit:
  единица — пиксель, ось y вниз), поэтому координаты ложатся на план без
  пересчёта. Штатный `L.CRS.Simple` направляет ось вверх и рисовал план
  отражённым — запись 14 в `DECISIONS.md`.
- `neighbors` — узлы того же этажа. Связь всегда двусторонняя: если `a`
  указывает на `b`, то и `b` указывает на `a`. Односторонних проходов в
  кампусе нет (запись 38), поэтому связь только в одну сторону — ошибка
  разметки: маршрут туда найдётся, а обратно нет. Вкладка «Проверка» в
  редакторе её показывает, «Исправить что можно» достраивает обратную связь.
- `isPortal` — точка смены этажа или корпуса: лестница, лифт, вход. Такие
  узлы навигатор рисует отдельным слоем со значком типа перехода, а редактор
  называет «точками переходов».
- `comment` — рабочая заметка разметчика. На маршрутизацию не влияет и
  студентам не показывается. Правится в карточке узла редактора,
  сохраняется в `graph.json`, участвует в отмене действий. Поле задумано
  именно под перенос официальных данных: помечать спорную геометрию,
  закрытые после определённого часа двери, приблизительные проходы.

### Переход

```json
{ "from": { "node": "a1_stairs" }, "to": { "node": "a2_stairs" }, "transition_type": "stairs" }
```

Типы: `entrance` (вход/выход из корпуса), `stairs`, `lift`, `bridge`
(переход между корпусами). Устаревшие `door`, `elevator`, `passage` читаются
и приводятся к каноническим, неизвестный тип становится `entrance` с
предупреждением — потерять связность графа из-за опечатки хуже, чем построить
маршрут через вход.

Переход соединяет узлы в обе стороны и попадает в список смежности графа.

### Алиасы

```json
{ "id": "a1_room101", "names": ["А-101", "Аудитория 101", "101", "Лекционная 101"] }
```

**Это единственный способ найти помещение в навигаторе.** Узел без алиаса
недостижим из интерфейса: его нельзя выбрать как начало или конец маршрута.
При переносе официальных данных алиасы обязательны для каждого узла, до
которого должен уметь дойти студент.

Поиск нечёткий: нормализует регистр и пунктуацию, ищет по номеру
(`«101» → a1_room101`), по префиксу и переживает набор в неверной раскладке
(`«,b,kbjntrf» → библиотека`). Номер, набранный латиницей, находит
кириллическую табличку: «A-305» → «А-305». Сворачиваются только буквы,
совпадающие и видом, и звучанием (`a e k m o t c x`), и «ё» в «е»; латинская
«B» остаётся транслитерацией «Б» и в «В» не превращается.

Номер находится в любом написании: «305», «А-305», «А305», «а 305», «a305».
Опечаток прощается тем больше, чем длиннее слово: одна в слове из 4–5 букв,
две — до 8 букв, три — дальше («бибилотека» → «Библиотека»); запросу короче
четырёх букв опечатки не прощаются. Разговорные слова — «столовка», «поесть»,
«туалет», «wc» — находят все места своей категории. Словарь этих слов живёт в
навигаторе (`search.categoryTerms` в `i18n/ru.ts` и `en.ts`), а не в данных.
Разговорное имя одного конкретного места («Спортзал», «Читалка») — это его
алиас в `names`.

**Одинаковые названия у разных помещений допустимы** — «Столовая» есть в
каждом корпусе. Такие помещения обе показываются в подсказках (с корпусом и
этажом), но точным вводом имени не выбираются: `AliasManager.resolve` для
неоднозначного имени возвращает `null`, а не случайный из вариантов. Если
помещение должно находиться набором одного слова, дайте ему уникальный алиас
(«Столовая корпуса А»). Список конфликтов отдаёт `ambiguousAliases()`.

### Категории мест

Категория говорит, что это за место: по ней навигатор ищет ближайшее место
нужного вида — туалет, столовую, гардероб или выход. Задаётся в записи алиаса:

```json
{ "id": "b1_canteen", "names": ["Столовая", "Буфет"], "category": "food" }
```

| Значение | Что это |
| --- | --- |
| `toilet` | туалет |
| `food` | столовая, буфет, кафе |
| `cloakroom` | гардероб для верхней одежды |
| `exit` | выход из корпуса или с территории: входы корпусов, проходная |

- Категория необязательна и одна на место. У аудитории её нет.
- Учитывается только у места с названием: безымянное место навигатору нечем
  назвать.
- Неизвестное значение загрузчик отбрасывает с предупреждением.
- Категория задаётся в данных, а не угадывается по названию: «Буфет» — еда, а
  «Раздевалка» бассейна — не гардероб. Подробности — запись 19 в
  `DECISIONS.md`.
- Редактор категории пока не правит, но сохраняет их при экспорте.

### Переводы названий

Навигатор двуязычный. Строки интерфейса переводит само приложение, а названия
из данных — поле `translations` рядом с исходным:

```json
{ "id": "b1_library", "names": ["Библиотека", "Читальный зал"],
  "translations": { "en": { "names": ["Library", "Reading room"] } } }
```

```json
{ "id": "building_a", "name": "Корпус А",
  "translations": { "en": { "name": "Building A" } }, "floors": [...] }
```

- Исходные `name` и `names` — на русском, языке данных. Перевод необязателен:
  без него интерфейс показывает исходное имя.
- Код языка — основной подтег строчными (`en`). `EN` и `en-US` загрузчик
  отвергает с предупреждением.
- Поиск находит помещение по имени на любом языке.
- Номер аудитории остаётся как на табличке («А-305»): набранный латиницей
  «A-305» поиск и так находит. Исключение — корпуса, чья буква не имеет
  латинского двойника (Б, Г, Д…): иностранный студент не наберёт «Б-201», и
  номер добавляется в английский перевод транслитерацией — «B-201».
- Редактор переводы пока не правит, но сохраняет их при экспорте.
  Подробности — запись 11 в `DECISIONS.md`.

### Именование узлов

Соглашение, которое уже соблюдено в данных и по которому удобно искать:

| Область | Шаблон | Примеры |
| --- | --- | --- |
| Территория | `campus_<место>` | `campus_gate`, `campus_square` |
| Этаж | `<корпус><этаж>_<место>` | `a1_room101`, `b2_lab`, `c1_pool` |
| Лестница/лифт | `<корпус><этаж>_stairs`, `_lift` | `a1_stairs`, `a2_lift` |
| Вход | `<корпус><этаж>_entrance` | `a1_entrance`, `a1_entrance_yard` |
| Переход в другой корпус | `<корпус><этаж>_bridge` | `a2_bridge`, `b2_bridge` |

Редактор для новых узлов предлагает автоматические id вида
`<корпус>_<этаж>_node_<номер>`. Их стоит переименовывать в понятные: по
автоматическим невозможно ни найти узел в данных, ни понять, что он
означает, — именно так в датасете однажды появился мусорный треугольник
`building_a_1_node_1/2/3`.

### Привязка к метрике кампуса

Пиксели разных планов между собой не сравнимы: у каждого плана своя система
координат. Чтобы у маршрута появились метры и секунды, планы привязываются к
территории кампуса. Все поля необязательные:

| Файл | Поле | Смысл |
| --- | --- | --- |
| `campus/meta.json` | `metersPerPixel` | масштаб плана территории — задаёт пространство кампуса в метрах |
| `buildings/<id>/meta.json` | `placement.metersPerPixel` | масштаб планов корпуса |
| | `placement.originMeters` | где на территории лежит левый верхний угол плана, `{ "x", "y" }` в метрах |
| | `placement.rotationDeg` | поворот плана относительно территории, градусы по часовой стрелке |
| | `placement.baseElevationMeters` | отметка пола **первого** этажа над уровнем территории |
| | `placement.floorHeightMeters` | высота этажа |
| запись в `floors` | `placement` | масштаб, сдвиг или поворот этого этажа поверх корпусных |
| | `elevationMeters` | отметка этого этажа, важнее формулы корпуса |

```json
{
  "id": "building_a",
  "name": "Корпус А",
  "placement": {
    "metersPerPixel": 0.05,
    "originMeters": { "x": 120, "y": 40 },
    "rotationDeg": 15,
    "baseElevationMeters": 0.6,
    "floorHeightMeters": 3.6
  },
  "floors": [{ "floor": -1, "elevationMeters": -3 }, { "floor": 1 }, { "floor": 2 }]
}
```

Соглашения, которые легко перепутать:

- ось y и у планов, и у территории направлена вниз, как на изображении;
  поворот идёт по часовой стрелке вокруг левого верхнего угла плана;
- отметка этажа `n` — `baseElevationMeters + (n − 1) · floorHeightMeters`: по
  номеру, а не по порядку в списке. Где номер с высотой не согласуется
  (нумерация без нулевого этажа, антресоль), нужен `elevationMeters`;
- привязка — только масштаб, поворот и сдвиг. Скоса и зеркалирования в
  формате нет.

Режим один на весь датасет. **Метрический** — если привязаны территория и
каждый этаж: стоимость маршрута в секундах, навигатор показывает время в пути.
**Пиксельный** — во всех остальных случаях: прежняя модель, времени в пути нет
вовсе. Значений по умолчанию нет: забытый масштаб не превращается в «метр на
пиксель», а переводит датасет в пиксельный режим, и загрузчик перечисляет,
чего не хватает каждому этажу. Тестовый `data/` привязан полностью (запись
30). Подробности и причины — запись 9 в `DECISIONS.md`.

С полной привязкой навигатор показывает **холст кампуса**: территория и этажи
всех корпусов на одной карте. Корпус открывается приближением — вместо крыши
проявляется его этаж, а шапка и колонка этажей показывают корпус, к которому
приближена камера; маршрут идёт одной линией с территории в здание, участки на
невидимых этажах просвечивают пунктиром (записи 31 и 32). Без привязки —
прежняя карта одного плана: территория или этаж корпуса.

---

## Как добавить корпус или этаж

Задача, которая чаще всего будет делегироваться. Данные правятся напрямую,
редактор для этого не обязателен.

**Новый этаж в существующем корпусе:**

1. Создать `data/buildings/<id>/floors/<номер>/` и положить туда план этажа
   (`map.png` или `map.svg` — формат в поле `planFormat`) и `graph.json` с
   узлами.
2. Добавить запись в `floors` в `data/buildings/<id>/meta.json`:
   `{ "floor": <номер>, "mapSize": { "width": <ширина>, "height": <высота> } }`.
   `mapSize` — размер плана в пикселях. Поле необязательное, но без него
   план до загрузки картинки показывается в резервных габаритах, а после
   загрузки карта перескакивает. Путей к файлам в записи нет: раскладка этажа
   фиксирована.
3. Связать этаж с соседними переходами в `data/transitions.json`
   (лестница или лифт). Без этого этаж окажется изолированной компонентой.
4. Прописать алиасы в `data/aliases.json` для всех узлов, доступных из
   поиска.
5. Проверить: `pnpm test` (тесты ядра читают реальный `data/` и падают на
   нарушении связности и нормализации), затем `pnpm dev` и посмотреть этаж
   в редакторе — панель диагностики покажет висячие ссылки и связи только
   в одну сторону.

**Новый корпус:**

1. `data/buildings/<id>/meta.json` с именем, списком этажей и
   `entranceFloor` — этажом, который открывается при выборе корпуса на карте
   кампуса (обычно этаж главного входа). Без поля навигатор берёт низший
   надземный этаж, а при нумерации с нуля это неверно.
2. Этажи — как выше.
3. Запись в `buildings` в `data/campus/meta.json`.
4. Узел входа на территории кампуса в `campus/graph.json` и переход типа
   `entrance` между ним и узлом `<id>1_entrance`.
5. Алиасы.

Ничего в коде менять не нужно: количество корпусов и этажей не зашито
нигде. Граф индексирует узлы по «корпус#этаж» в конструкторе, поэтому
рост объёма данных не меняет стоимость типовых операций.

### Что есть в тестовых данных

Тестовый кампус в `data/` строит генератор из описания в метрах
(`tooling/lib/test-campus.mjs`): планы этажей и территории — SVG, а графы и
привязка к метрике получаются из той же геометрии. Поэтому узлы стоят в своих
помещениях, а навигатор работает в метрическом режиме и показывает время в
пути. Названия, переводы и категории (`aliases.json`) — ручная разметка,
генератор их не трогает. После правки описания:

```bash
node tooling/generate-test-data.mjs
```

Правку тестового графа в редакторе следующий запуск генератора перезапишет —
править нужно описание (запись 30 в `DECISIONS.md`).

По тестовым данным удобно проверять руками:

| Сценарий | Что попробовать |
| --- | --- |
| Лифт только в корпусе А и только до 2 этажа | «Деканат» (А, 2 этаж) с ограничением «без лестниц» — лифтом |
| Без лестниц не дойти | «А-305» (А, 3 этаж) без лестниц: маршрут не найден, в шторке кнопка «Разрешить лестницы» |
| Переход между корпусами | «Компьютерный класс» (Б, 2 этаж) без лестниц — лифтом А и переходом |
| Два входа в корпус А | «Главный вход корпуса А» и «Вход со двора» от «Парковки» |
| Одноимённые помещения | «Столовая», «101» — в корпусах А и Б |
| Переводы | «Library», «Canteen»; «B-201» — номер корпуса Б латиницей |
| Латиница и «ё» | «a-305», «зал легкой атлетики» |
| Рабочие заметки | `comment` у «Входа со двора» и «Б-204» — видны в редакторе |
| Повёрнутый корпус | Корпус В стоит под углом 20° к территории |

Для проверки на объёме 5×11 — генератор синтетического набора (раздел «Куда
разворачивается»).

### Замена тестовых планов на официальные

Тестовые планы и графы — схема, а не копия реальных корпусов. При замене:

1. Положить официальные планы как `map.png` или `map.svg`, формат указать в
   `planFormat` (раздел «Формат плана»), размеры — в `mapSize`: у этажа — в
   `floors` в `meta.json` корпуса, у территории — в `campus/meta.json`.
   Пиксельную геометрию карта всё равно берёт из самого файла (`useImageSize`
   в mapkit), поэтому устаревший размер маршрут не сломает — он даст только
   скачок карты в момент загрузки плана.
2. Расставить узлы по новым планам. Проще всего это делать в редакторе: он
   показывает план с реальной геометрией, есть привязка к сетке и групповое
   перемещение. Узел помещения — внутри у двери, а не на номере аудитории:
   последний отрезок маршрута идёт от двери к узлу и перечеркнул бы подпись.
3. Узлы, в которых геометрия пока приблизительная, помечать полем `comment`
   — оно для этого и существует.
4. Привязать планы к территории (раздел «Привязка к метрике кампуса»). Без
   этого навигатор строит маршруты, но не показывает время в пути.
5. Удалить генератор тестового кампуса (`tooling/generate-test-data.mjs` и
   `tooling/lib/test-campus.mjs`): его запуск перезаписал бы разметку.

Что учесть на холсте кампуса (записи 31–35):

- SVG показывается изображением, а не встраивается в страницу: скрипты файла
  не исполняются, одинаковые `id` градиентов и штриховок из выгрузок САПР не
  перекрывают друг друга, а подписи подробного плана не раскладываются заново
  на каждом кадре масштаба.
- Поэтому официальный SVG должен быть самодостаточным: внешние картинки,
  веб-шрифты и стили по ссылкам изображение не загружает. Растровые вставки
  встраиваются как `data:`, подписи — системными шрифтами или контурами.
- Перекрашивать план в тёмной теме умеют только файлы с классами генератора
  (`svg.campus-plan`, `plan-room`, `plan-wall`…): стиль темы вписывается в план
  перед показом (`apps/viewer/src/theme/planTheme.ts`). Остальные планы —
  растровые и векторные — в тёмной теме инвертируются фильтром.
- Холст поворачивается двумя пальцами, мышью с правой кнопкой или Alt и
  жестом тачпада Mac в Safari; компас в правой колонке возвращает на север
  (запись 36). Подписи внутри планов поворачиваются вместе с изображением.
- Фон холста вокруг кампуса — цвет травы плана территории (токен
  `--canvas-ground` в `apps/viewer/src/index.css`, светлая и тёмная тема): с
  официальным планом его стоит поставить в цвет территории на нём, иначе край
  плана будет виден на фоне.
- `mapSize` этажей стоит заполнить: по нему холст знает контур корпуса до
  загрузки плана, без него контур — прямоугольник вокруг узлов входного этажа.
- Когда крыша тает в этаж и с какого масштаба видны значки территории — доли
  экрана и порог в `apps/viewer/src/utils/canvasReveal.ts`; их стоит проверить
  на реальных корпусах. На карте держится не больше 12 планов этажей сразу.

---

## Как работать в редакторе

Редактор запускают из репозитория (`pnpm dev`, адрес `:3001`): тогда
«Сохранить» пишет правки прямо в `data/`, их сразу видит навигатор, а
забирает git. Подробности решений — записи 38 и 39 в `DECISIONS.md`.

**Что где.** Карта в середине, вокруг — закреплённые колонки, и ни одна
ничего на карте не закрывает:

```
┌ Редактор кампуса · ↶ ↷ · Проверка (3) · Поиск · ? · файлы ──────────────┐
│ Структура   │Инстр.│ Выбор: подсказка и действия с выбранным │ Инспектор │
│  Территория │ Выбор│                                         │ Свойства  │
│  ▾ Корпус А │ Узел │                КАРТА                    │ Проверка  │
│     Этаж 1  │ Связь│                                         │ Маршрут   │
│  Показывать │ ...  │                                         │           │
└ Корпус А / Этаж 1 · на плане 23 узла · x 120 · y 102 · Сохранено ───────┘
```

Боковые колонки сворачиваются стрелкой в заголовке — карте больше места;
браузер это запоминает.

**Мышь.** Щелчок по узлу выбирает его и открывает карточку справа; двойной
щелчок сразу ставит курсор в название; перетаскивание узла двигает его
(выделение — целиком); Shift или Ctrl со щелчком добавляют узел к выбору;
Shift с протягиванием по пустому месту — рамка выделения; перетаскивание
пустого места двигает карту. Правая кнопка открывает меню: у узла,
выделения, связи, перехода и пустого места — своё. Отметка перехода на
другой план («↑ этаж 2») ведёт щелчком к другому концу.

**Клавиши** работают в любой раскладке — читается физическая клавиша:

| Клавиша | Действие |
| --- | --- |
| V, N, E, T, L | инструменты: выбор, узел, связь, переход, линия |
| 1–8 | выбрать вид точки (коридор, помещение, туалет…) и взять инструмент «Узел» |
| Ctrl+Z, Ctrl+Y (Ctrl+Shift+Z) | отменить, повторить; кнопка отмены называет действие, отмена открывает нужный план |
| Ctrl+C, Ctrl+V, Ctrl+D, Ctrl+A | копировать, вставить на открытый план, дублировать, выделить всё на плане |
| Delete | удалить выделенное |
| Стрелки (с Shift — крупнее) | сдвинуть выделенное; со включённой сеткой — по клетке; без выделения — сдвинуть карту |
| PageUp, PageDown | этаж выше, ниже в открытом корпусе |
| Ctrl+F, Ctrl+S, Esc | поиск, сохранить, отменить начатое и снять выбор |
| Enter | закончить начатую линию коридора |
| F1 | справка: мышь, клавиши и короткие рецепты |

**Поиск** (Ctrl+F) ищет так же, как навигатор: прощает опечатки, латинские
буквы вместо русских («a-101») и набор в другой раскладке, понимает id и
координаты «100, 200». В списке видно место словами и название, по
которому узел нашёлся.

**Виды точек.** Инструмент «Узел» ставит точку выбранного вида: вид сам даёт
название по шаблону, цепляет точку к ближайшей на плане, отмечает точкой
перехода, ставит вид места и, если так задано, повторяет точку на всех этажах
корпуса и связывает их переходами. Вид выбирается цифрой 1–8 или щелчком в
строке над картой; «Все виды…» открывает окно, где вид можно создать, изменить
и удалить. Каталог видов лежит в `data/place-kinds.json` и едет в git вместе с
разметкой. Один щелчок — одна отмена: Ctrl+Z убирает всё, что он сделал.

Вид «Коридор» ведёт линию: каждый щелчок ставит точку и связывает её с
предыдущей, пунктир показывает, куда пойдёт следующая связь, Enter или Esc
заканчивают линию.

**Переход между этажами**: инструмент «Переход» (T), тип в строке над
картой, щелчок по узлу, смена этажа в «Структуре» или PageUp/PageDown,
щелчок по второму узлу. Узлы одного плана соединяются связью, а не
переходом — редактор об этом скажет.

**Вид места** (туалет, еда, гардероб, выход) ставится в карточке узла у места
с названием: по нему навигатор показывает быстрые кнопки «ближайший туалет» и
«где поесть» и значок на карточке места. Раньше вид можно было вписать только
руками в `aliases.json`.

**Соседний этаж бледно** («калька») накладывает план и точки соседнего этажа
поверх открытого: лестницы, лифты и туалеты встают друг над другом, а
расхождение планов видно сразу. Привязка к метрике для этого не нужна —
калька рисуется в координатах плана.

**Точность.** Новая точка сама встаёт в один ряд с соседней, если целиться
почти в него: линии выравнивания видно до щелчка, Alt при щелчке ставит точку
ровно туда, куда нажали. Сетка осталась необязательной настройкой рядом —
двери на официальных планах стоят там, где начерчены, а не в узлах клеток.

**Что показывать.** В левой колонке — названия узлов (появляются, когда
узлы на экране не слипаются), точки переходов, связи, переходы, подсветка
проблем и сетка. Без выбранного узла инспектор показывает обзор плана:
сколько узлов и названий, есть ли узлы без связей и не распался ли план на
не связанные между собой части.

**Сохранение.** «Сохранить» (Ctrl+S) сначала проверяет данные и при ошибках
спрашивает, сохранять ли всё равно. Пишутся только изменившиеся файлы в
том же виде, что у генератора тестовых данных. Если файл на диске изменился
после открытия редактора (git, другая вкладка), сохранение откажет, а не
затрёт чужую правку. Развёрнутый без dev-сервера редактор сохраняет только
«Скачать архив» — его содержимое распаковывается в корень репозитория.

**Несохранённое не теряется**: пока правки не сохранены, они пишутся
черновиком в браузер, и при следующем открытии редактор предлагает их
восстановить; при закрытии вкладки браузер предупреждает.

---

## Ссылки на навигатор и QR-коды

Навигатор открывается с готовым маршрутом или точкой «вы здесь» по ссылке:

| Ссылка | Что откроется |
| --- | --- |
| `?from=campus_gate&to=b2_lab` | маршрут, карта на этаже начала |
| `?at=a1_entrance` | начало маршрута в этой точке, карта на её этаже — для QR-кода у входа |
| `?at=a1_entrance&lang=en` | то же на английском — для английской таблички |
| `?to=b2_lab` | карточка места на его этаже, оттуда один шаг до маршрута — для указателя «как пройти» |

- В параметрах — **id узлов**: они стабильны и однозначны. Название тоже
  принимается, если ровно одно помещение так называется.
- Если точки нет в данных (устаревший QR-код после переразметки), навигатор
  так и пишет, а не открывает пустую карту.
- Адресная строка повторяет текущий маршрут, поэтому «Поделиться» и
  перезагрузка вкладки его сохраняют.
- Базовый путь развёртывания (`CAMPUS_BASE_PATH`) в ссылке сохраняется:
  `https://вуз.example/map/?at=a1_entrance`.

Геолокации нет и не будет: в помещении она не работает, а у планов нет
географической привязки. QR-код у двери — это и есть «где я».

---

## Ключевые инварианты

Нарушение любого из них уже приводило к дефектам.

**`Graph` неизменяемый.** Все индексы строятся один раз в конструкторе:
`nodes` (id → узел), `nodesByFloor` («корпус#этаж» → узлы),
`transitionsByKey` (ключ ребра → переход), `adjacency` (id → объединённый
список соседей). Мутационного API нет намеренно — он позволял
рассинхронизировать индексы с содержимым. Редактор хранит собственное
мутабельное состояние и собирает новый `Graph` перед каждым поиском пути:
`new Graph(nodes.values(), transitions)`.

**Связность считает только ядро.** `findConnectedComponents` — единственный
обход графа. Собственная копия в редакторе для каждого узла перебирала весь
массив переходов, то есть работала за O(N·T).

**Область видимости — доменное понятие.** `ViewScope` — либо
`{ mode: 'campus' }`, либо `{ mode: 'floor', buildingId, floor }`. Правило
«какие узлы показывать» задано один раз: `scopeOfFloor` (из выбранного
этажа), `scopeOfNode` (из узла), `isNodeInScope` (фильтр). И слои карты
навигатора, и оверлеи редактора пользуются ими.

**Состояние не дублируется.** Производные значения вычисляются, а не
хранятся. В навигаторе нет отдельного флага режима просмотра: он полностью
выводится из `activeFloor`. В редакторе комментарий узла живёт в
`MapNode.comment`, а не в отдельной карте. Хранимый дубль источника истины
рано или поздно расходится.

**Опции маршрутизации по умолчанию — в ядре.**
`DEFAULT_PATHFINDING_OPTIONS`. Свой экземпляр в приложении заводить не надо.

**Метры и пиксели не смешиваются.** Мировые координаты узлов живут в `Graph`
(`isMetric`, `getWorld`), а не в `MapNode`: пиксели узла редактор меняет
перетаскиванием, и копия на узле устарела бы. Если не привязан хоть один
этаж, пиксельный весь граф. Модель стоимости (`pathfinding/costModel.ts`)
устроена так, что каждый шаг стоит не меньше прямого расстояния, делённого на
самую быструю скорость, — на этом держится оптимальность A*. Поэтому
предпочтение выражается штрафом («лестница дороже»), а не скидкой («лифт
дешевле»), и время в пути (`durationSeconds`) — отдельное поле, а не
стоимость, которую минимизировал поиск (`cost`).

**Ядро не печатает в консоль.** Проблемы в данных возвращаются списком
`warnings` из `loadDataset`, а что с ними делать, решает приложение:
навигатор пишет их в консоль, редактор показывает в панели диагностики.

**Никаких внешних CDN.** Стили Leaflet импортируются из npm-пакета, шрифты —
системные. Сервис, развёрнутый у вуза, обязан работать в изолированной сети,
а загрузка шрифта с `fonts.googleapis.com` означала ещё и передачу IP-адреса
третьей стороне, что для развёртывания в ЕС неприемлемо.

---

## Проверки

**Тесты ядра** — `packages/core/tests`, Vitest. Кроме базовых проверок
(`dataset`, `graph`, `pathfinding`, `connectivity`, `aliases`, `scope`,
`projection`) есть наборы, которые закрывают найденные дефекты и обязаны
проходить при любой правке соответствующего кода:

- `pathfinding.optimality` — A* сравнивается с независимо написанной Дейкстрой
  на случайных многоэтажных графах, в пиксельном и метрическом режимах.
  Именно его отсутствие скрыло то, что поиск возвращал неоптимальные маршруты
  (запись 6 в `DECISIONS.md`);
- `pathfinding.consistency` — каждый шаг сгенерированных графов и реального
  датасета с условной привязкой стоит не меньше прямого пути между его
  концами: на этом условии держится оптимальность A* с метрической
  эвристикой (запись 9);
- `dataset.loader` — поведение загрузчика на битых данных, разбор привязки к
  метрике и независимость результата от порядка прихода ответов при
  параллельной загрузке;
- `aliases.collisions` — одноимённые помещения в разных корпусах.

Каталог `tests` проверяется типами через `tsconfig.test.json`: основной проект
ядра `composite` с `rootDir: src`, и тесты в него не добавить.
Импортируют исходники, а не `dist`: Vitest резолвит `.js`-спецификаторы в
`.ts`, поэтому сборка перед прогоном не нужна.

Тесты кода работают на синтетическом кампусе `tests/helpers/sampleDataset.ts`
— файлы в памяти, которые грузятся тем же `loadDataset`, что и в продукте.
Продуктовый `data/` правит команда разметки, и тест с числами «3 корпуса, 21
алиас» ломался бы от работы, ради которой существует редактор. Реальные данные
читает только `dataset.real.test` — инварианты без количеств (ноль
предупреждений, связность, каждое название ведёт к существующему узлу) — и
проверка согласованности эвристики на их топологии. Случайные многоэтажные
кампусы для проверок поиска строит `tests/helpers/generatedCampus.ts`.

Отдельно зафиксирован инвариант, который раньше был нарушен: сумма
стоимостей шагов маршрута обязана равняться `PathResult.cost` при любых
опциях, включая `preferLift`.

**Тесты редактора** — `apps/editor/tests`, Vitest без DOM, на синтетическом
кампусе `tests/helpers/fixture.ts`. Главный набор — `history`: каждое
действие правки проверяется кругом «сделать → отменить → повторить», и данные
обязаны возвращаться ровно к прежним. Новое действие редактора добавляется в
этот список вместе с членом `HistoryEntry`. Интерфейс редактора проверяют
сценарии в браузере.

**Смоук-тесты раздачи** — `tooling/smoke-test.mjs`, запускаются как
`pnpm test` в приложениях. Поднимают прод-сборку (33 проверки у навигатора,
25 у редактора) и проверяют оболочку, PWA-ассеты, данные, честный 404
вместо SPA-fallback, отказ на обход каталога и работу клиентской
маршрутизации. Запросы идут через `node:http` с дословным `path`: WHATWG URL
нормализует `..` до отправки, и проверки обхода каталога стали бы
бесполезными.

Проверить раздачу в dev-режиме вручную:
`node tooling/smoke-test.mjs --app viewer --mode dev`.

Тот же набор под подкаталогом — базовый путь передаётся отдельно и намеренно
не выводится из окружения: сборка и проверка это разные запуски, и молчаливое
расхождение между ними дало бы зелёный прогон на неверно собранном приложении.

```bash
CAMPUS_BASE_PATH=/campus/ pnpm --filter @campus-map/viewer build
node tooling/smoke-test.mjs --app viewer --mode prod --base /campus/
```

**Сверка датасета** — `tooling/verify-dist-data.mjs` сравнивает `dist/data` с
корневым `data/` по содержимому и отдельно ловит **лишние** файлы: именно
лишний файл выдаёт вернувшуюся копию данных внутри приложения.

**Сценарии в браузере** — `tooling/browser-check.mjs`, команда
`pnpm check:browser` после `pnpm build`. Поднимает прод-сборку каждого
приложения, запускает установленный Chrome или Edge без окна и через протокол
отладки (CDP) проходит сценарии из `tooling/browser/scenarios/`: поиск,
карточка места, маршрут, пошаговая навигация, холст кампуса (крыши, этаж при
приближении, текущий корпус, просвечивающий маршрут), недавние места, быстрые кнопки
к ближайшему месту и разговорный поиск, знакомство при первом запуске, работа без
связи, обновление по согласию, контраст текста
в светлой и тёмной теме и переключатель темы, «Поделиться»,
ссылки для QR-кодов; в редакторе — мышь и контекстное меню, клавиатура в
любой раскладке, живая карточка узла и обзор плана, переходы между этажами,
проверка маршрута, сохранение в `data/` и черновик, колонки вокруг карты
(ничто не закрывает план, карта не прыгает, свёрнутые колонки отдают ей
место), справка и поиск с опечатками, читаемость и размеры (контраст
каждого текста, кнопки от 44 пикселей, поля ввода от 16), значки.
Узлы, связи и переходы на карте редактора помечены `data-node-id`,
`data-edge` и `data-transition`, а разметка интерфейса — `aria-label`
(«Свойства узла», «Параметры инструмента»), `data-tool-name` и
`data-status-place`: по ним сценарии находят элементы на экране
(`tooling/browser/editor.mjs`). Сценарий с `isolatedData` работает на копии
`data/` со своим dev-сервером и читает сохранённые файлы с диска — так
проверяется сохранение, не трогая канонический датасет. После каждого шага
проверяются консоль и экран ошибки. Сценарии написаны под тестовый `data/` и
находят элементы по подписям, поэтому правка строк интерфейса или тестовых
данных требует правки сценария.

- без сборки, на dev-сервере: `node tooling/browser-check.mjs --mode dev`;
- один набор: `--app viewer --only навигация`;
- снимки экрана: `--shots .local/shots`;
- другой браузер: переменная `CAMPUS_BROWSER`.

В `pnpm test` и CI не входит: нужен установленный браузер, и прогон идёт
минуты. Причины и условия — запись 18 в `DECISIONS.md`.

**CI** — `.github/workflows/ci.yml`: установка с `--frozen-lockfile`,
сборка, типы, линтер, тесты.

---

## Как поставить задачу

Что где менять, чтобы задача не ушла не туда.

| Задача | Где |
| --- | --- |
| Логика маршрута, типы переходов | `packages/core/src/pathfinding/astar.ts`, `types/transition.ts` |
| Стоимость шага, время в пути, эвристика | `packages/core/src/pathfinding/costModel.ts` |
| Привязка планов к территории, мировые координаты | `packages/core/src/projection.ts` |
| Формат данных, нормализация, новые поля датасета | `packages/core/src/types/`, `dataset/loader.ts` |
| Поведение карты как таковое (зум, подложка, границы) | `packages/mapkit/src/mapFrame.tsx`, `PixelMap.tsx`, `WorldMap.tsx` |
| Холст кампуса: крыши, этажи, камера, текущий корпус | `apps/viewer/src/components/Map/WorldCanvas.tsx`, `CanvasPlans.tsx`, `CanvasCamera.tsx`; запись 32 |
| Интерфейс навигатора, панели, карточка маршрута | `apps/viewer/src/components/UI/` |
| Слои навигатора (маркеры, порталы, линия маршрута) | `apps/viewer/src/components/Map/` |
| Пошаговые инструкции маршрута | `apps/viewer/src/utils/routeInstructions.ts` |
| Строки интерфейса, язык (`?lang=en` в адресе) | `apps/viewer/src/i18n/`, `stores/settingsStore.ts`; запись 12 в `DECISIONS.md` |
| Имена корпусов и помещений на языке интерфейса | переводы в данных (раздел «Переводы названий»), `buildingName` и `AliasManager` ядра |
| Раскладка редактора: шапка, структура, инструменты, строка параметров, инспектор | `apps/editor/src/components/Layout/` |
| Слои карты редактора: узлы, связи, переходы, подписи, маршрут | `apps/editor/src/components/Map/` |
| Карточка узла, обзор плана, проверка, маршрут, поиск, справка | `apps/editor/src/components/UI/` |
| Поиск узлов с опечатками, сводка плана | `apps/editor/src/utils/nodeSearch.ts`, `planStats.ts` |
| Цвета редактора и карты | токены в `apps/editor/src/index.css`, чтение значением — `utils/themeColor.ts` |
| Действия редактора и отмена | срезы стора `apps/editor/src/stores/editor/`, сборка — `editorStore.ts`; запись истории — `historyStore.ts`, её применение — `editor/historyApply.ts` |
| Сохранение: файлы датасета, запись в `data/`, архив, черновик | `apps/editor/src/utils/datasetFiles.ts`, `diskStore.ts`, `exportData.ts`, `draftStorage.ts`, срез `stores/editor/storageSlice.ts`; запись на диск — `tooling/vite-plugin-campus-data.mjs` (`writable`) |
| Виды точек (заготовки разметчика) | `data/place-kinds.json`, тип — `packages/core/src/types/placeKind.ts`, встроенный набор — `apps/editor/src/utils/placeKinds.ts` |
| Импорт датасета из архива | `apps/editor/src/utils/importZip.ts` |
| Проверка данных для вкладки «Проверка» | `apps/editor/src/utils/validateData.ts`, автоисправление — `autoFix.ts`, отчёт для интерфейса — `hooks/useValidationReport.ts` |
| Раздача данных, сборка, PWA, базовый путь развёртывания | `tooling/`, `apps/*/vite.config.ts` |
| Архитектурное решение и его причины | `DECISIONS.md` |

Два момента, которые стоит проговорить в задаче явно:

- **Новое поле данных** требует правки в четырёх местах, иначе оно
  потеряется: тип в `types/`, чтение в `dataset/loader.ts`, запись в
  `apps/editor/src/utils/exportData.ts` и тест в `packages/core/tests/`.
  Поле, которое читается, но не пишется экспортом, молча исчезает при
  первом же сохранении из редактора.
- **Новое отменяемое действие** в редакторе добавляется как член объединения
  `HistoryEntry` в `stores/historyStore.ts`. Компилятор сам потребует
  обработать его в обеих ветках — и в отмене, и в повторе.

### Что известно как незакрытое

- Компоненты редактора модульными тестами не покрыты — только сценариями в
  браузере; стор и чистые функции проверяет `apps/editor/tests`.
- Расстановка множества разнообразных точек (виды точек, привязки, быстрые
  инструменты) — следующая тема редактора; владелец просил продумать её
  отдельным предложением до работы.
- Корпуса, этажи и планы в редакторе пока не создаются: привязка планов к
  метрике правится в `meta.json` руками.
- Инструмента привязки к метрике в редакторе нет — поля пишутся в `meta.json`
  вручную. Числа в `WALKING_PROFILE` — оценки, их стоит сверить с замерами.
- `data/` — сгенерированный тестовый кампус: три корпуса, шесть этажей,
  схематичные SVG-планы. Архитектура на объём не завязана, но узлы при замене
  планов придётся расставить заново.

---

## Частые проблемы

**Приложение не видит правку ядра.** Ядро подключается собранным:
`pnpm --filter @campus-map/core build`, либо держать рядом
`pnpm --filter @campus-map/core dev` (watch).

**Навигатор не показывает время в пути.** Датасет в пиксельном режиме: не
привязана территория или хотя бы один этаж. Если привязка задана частично,
загрузчик перечисляет в предупреждениях (консоль навигатора, диагностика
редактора), чего не хватает каждому этажу. Проверить метрический режим, не
трогая `data/`, можно на копии через `CAMPUS_DATA_DIR`.

**Данные отдаются как HTML.** Значит запрос к `/data/...` не попал в плагин.
Проверить, что `sourceDir` в `vite.config.ts` указывает на `data/` в корне
репозитория, и прогнать `node tooling/smoke-test.mjs --app <app> --mode dev`.

**PWA-иконки битые или отсутствуют.** Ассеты в `apps/viewer/public/`
сгенерированы `tooling/generate-pwa-assets.mjs` и закоммичены, чтобы сборка
не зависела от шага кодогенерации. При смене фирменного стиля —
перезапустить генератор, а не править PNG вручную.

**`pnpm install` падает с ошибкой lock-файла.Lock-файл версионируется, и CI
ставит зависимости с `--frozen-lockfile`. После изменения зависимостей
закоммитить обновлённый `pnpm-lock.yaml`.

**Стиль строк и кодировок.** `.editorconfig` задаёт LF и UTF-8 без BOM.
Файлы данных читаются с учётом возможного BOM, но писать их нужно без него.
